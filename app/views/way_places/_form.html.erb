<%= form_for(@way_place) do |f| %>
  <% if @way_place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@way_place.errors.count, "error") %> prohibited this way_place from being saved:</h2>

      <ul>
      <% @way_place.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :route_id %><br />
    <%= f.select :route_id, Route.trip_routes(@trip_id) %>
  </div>
  <div class="field">
    <%= f.label :place_id %><br />
    <%= f.collection_select :place_id, Place.where(trip_id: @trip_id), :id, :name %>
  </div>
    <div class="field">
    <%= f.label :place_kind %><br />
    <%= f.select :place_kind, ["way_place", "start_place", "end_place"] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


